#include<stdio.h>
#include<conio.h>
int main()
{
float a,b,c;
float add(float,float);

printf("enter 2 nos.",c);
scanf("%f%f",&a,&b);
c=add(a,b);
printf("answer=%f",c);
}
float add(float x,float y)
{
    return (x+y);
}

